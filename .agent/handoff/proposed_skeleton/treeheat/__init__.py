"""treeheat — tree thermal-safety simulation pipeline (clean rebuild).

Layered, config-driven pipeline:
    config -> io -> radiance -> physics(engines) -> risk -> viz

This is a PLACEHOLDER skeleton for a new agent team. No logic is ported.
Every module names the archive file to port FROM and the design contract to
honour. See ../03_consolidation_recommendations.md and ../04_archive_map.md.
"""
__version__ = "0.0.0"
